tasks:
  Split orders file:
    robotTaskName: Split orders file
  Process order:
    robotTaskName: Process order
  Read order info:
    robotTaskName: Read order info

artifactsDir: output
condaConfigFile: conda.yaml
ignoreFiles: [.gitignore]

PATH: [.]
PYTHONPATH: [.]
